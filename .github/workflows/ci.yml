name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Configurar Python 3.8
      uses: actions/setup-python@v5
      with:
        python-version: '3.8'
    - name: Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Análisis de código estático (flake8)
      run: flake8 .
    - name: Análisis de seguridad (bandit)
      run: bandit .
    - name: Ejecutar tests
      run: pytest
